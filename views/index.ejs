<%- include('./partials/html-head') %>
<%- include('./partials/nav') %>

<main>

  <% if (user?.profile.avatar) { %>
    <div>
      <img 
        src="<%= user.profile.avatar %>" 
        alt="<%= user.profile.name %> avatar"
      >
    </div>
  <% } %>
  <h2>Bienvenue</h2>
  <h4>Friend to Soccer-team-tracker</h4>
</main>

<%- include('./partials/footer') %>
