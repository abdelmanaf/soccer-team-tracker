<%- include('./partials/html-head') %>
<%- include('./partials/nav') %>

<main>

  <% if (user?.profile.avatar) { %>
    <div>
      <img 
        src="<%= user.profile.avatar %>" 
        alt="<%= user.profile.name %> avatar"
      >
    </div>
  <% } %>
  <h3>Bienvenue, Friend to Soccer-team-tracker</h3>
</main>

<%- include('./partials/footer') %>
